#include <curses.h>
#include <iostream>

#include "vect.h"

int main() {
	using namespace std;

	Vec3D v({1, 2, 3});

	/*initscr();
	cbreak();
	noecho();
	keypad(stdscr, TRUE);
	nodelay(stdscr, TRUE);
	curs_set(0);


	while(true) {
		wrefresh(stdscr);
		mvwaddch(stdscr, 10, 10, 'A');
	}

	endwin();*/
}
